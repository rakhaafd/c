#include <stdio.h>
#include <math.h>

int main(){
    int num_base = 2;
    int num_pow = 3;
    int result = pow(num_base, num_pow);
    printf("your num base is %d, and pow number is %d, result is .. %d", num_base, num_pow, result);
    return 0;
}